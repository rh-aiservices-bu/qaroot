---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
type: Opaque
stringData:
  username: "qaroot"
  password: "changeme"
  database: "qaroot_mvp"
  connection-string: "postgresql://qaroot:changeme@postgresql:5432/qaroot_mvp"
---
apiVersion: v1
kind: Secret
metadata:
  name: app-credentials

type: Opaque
stringData:
  # Default admin credentials (change after first login)
  admin-username: "admin"
  admin-password: "changeme123"
  admin-email: "admin@university.edu"

  # JWT signing secret (generate with: openssl rand -base64 32)
  jwt-secret: "your-random-jwt-secret-here-min-32-chars"

  # Session secret (generate with: openssl rand -base64 32)
  session-secret: "your-random-session-secret-here-min-32-chars"
